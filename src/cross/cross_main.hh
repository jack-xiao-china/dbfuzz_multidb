/// @file
/// @brief Cross-mode entry point (placeholder for Phase 3)

#ifndef CROSS_MAIN_HH
#define CROSS_MAIN_HH

#include "core/dbms_info.hh"
#include <map>
#include <string>

using namespace std;

void cross_run(dbms_info &d_info, map<string, string> &options);

#endif
